#
# Copyright The Titan Project Contributors.
#

#
# When a tag is pushed, build and upload the binary to the in-progress draft release.
#
name: Release

on: create

permissions:
  contents: write

jobs:
  upload:
    name: Build and Upload
    strategy:
      matrix:
        os: [ ubuntu-latest ]
    runs-on: ${{ matrix.os }}
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.21'
      - name: Get Draft Release
        id: draft_release
        run: |
          echo "upload_url=$(curl -sH "Authorization: token $GITHUB_TOKEN" https://api.github.com/repos/$GITHUB_REPOSITORY/releases | jq -rc '.[] | select(.draft) | .upload_url')" >> $GITHUB_OUTPUT
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Get Binary Name
        id: binary_name
        run: ./.github/get-binary-name ssh ${{ runner.os }}
      - name: Build
        run: go build -o build/ssh ./cmd/ssh
      - name: Test
        run: go test ./...
      - name: Upload Artifact
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.draft_release.outputs.upload_url }}
          asset_path: ./build/ssh
          asset_name: ${{ steps.binary_name.outputs.binary_name }}
          asset_content_type: application/octet-stream

name: Repo Health

on:
  push:
    branches: [master]
  pull_request:
    branches: [master, main]

jobs:
  health:
    uses: datadatdat/.github/.github/workflows/repo-health.yml@master
    with:
      language: go
      coverage-floor: 100
      max-cyclomatic: 25
      mode: warn
    secrets:
      GO_MODULES_TOKEN: ${{ secrets.GO_MODULES_TOKEN }}
